@model TimelineViewModel
@{
    Layout = "Shared/_Layout.cshtml";
}
<div class="container">
    @if (Model.ShowTimeline)
    {
        foreach (var entry in Model.Timeline)
        {
            @Html.Partial("../Timeline/_TimelineEntry", entry)
        }
    }
</div>